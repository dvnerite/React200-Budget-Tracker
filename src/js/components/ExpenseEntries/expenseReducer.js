


const defaultState = {
    description: '',
    amount: '',
    lineitems: []

};

export default function ExpenseReducer (state = defaultState, action) {

    // the `state = default state` above is new ES6 syntax
    // for defining a default value on a parameter
    return state;
}